//write a cpp program to calculate area of rectangle using user inputs.

#include<iostream>
using namespace std;

int main()
{
    float length=0,breadth=0,breadth;

    cout<<"Enter the length of the rectangle : ";
    cin>>length;
    cout<<"Enter the breadth of the rectangle : ";
    cin>>breadth;
       cout<<"Enter the breadth of the rectangle : ";
    cin>>breadth;
    cout<<"The Area of the Rectangle is"<<length*breadth;
    return 0;
}
